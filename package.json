{
  "name": "project-capital-stream",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "serve src",
    "start": "serve dist",
    "build": "shx rm -rf dist && shx mkdir dist && shx cp -r src/* dist/",
    "check-types": "echo 'No types to check'"
  },
  "devDependencies": {
    "serve": "^14.2.4",
    "shx": "^0.3.4"
  }
}