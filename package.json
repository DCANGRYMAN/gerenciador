{
  "name": "project-capital-stream",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "serve public",
    "prebuild": "shx rm -rf public && shx mkdir public",
    "build": "shx cp -r src/* public/"
  },
  "devDependencies": {
    "serve": "^14.2.4",
    "shx": "^0.3.4"
  }
}