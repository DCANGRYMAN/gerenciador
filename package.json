{
  "name": "project-capital-stream",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "serve dist",
    "prebuild": "shx rm -rf dist && shx mkdir dist",
    "build": "shx cp -r src/* dist/"
  },
  "devDependencies": {
    "serve": "^14.2.4",
    "shx": "^0.3.4"
  }
}